/************************************************************************* > EE569 Homework Assignment #1 > Date:     February 4, 2017 > Author:   Chenyu Peng > ID:       3498-8893-91 > email:    chenyupe@usc.edu > Compiled on OS X with gcc > Input: ./dog_mirroring dog.raw dog_mirroring.raw 300 300 3 ************************************************************************/#include <stdio.h>#include <iostream>#include <stdlib.h>using namespace std;int main(int argc, char *argv[]){	FILE *file;	int BytesPerPixel = 1;	int Length = 0;	int Width = 0;		if (argc < 5){		cout << "Syntax Error - Incorrect Parameter Usage:" << endl;		cout << "program_name input_image.raw output_image.raw Length Width [BytesPerPixel = 1]" << endl;		return 0;	}		if (argc < 6){		BytesPerPixel = 1; // default is grey image		Length = atoi(argv[3]);		Width = atoi(argv[4]);	} 	else {		BytesPerPixel = atoi(argv[5]);		Length = atoi(argv[3]);		Width = atoi(argv[4]);	}		unsigned char Imagedata[Width][Length][BytesPerPixel];	if (!(file=fopen(argv[1],"rb"))) {		cout << "Cannot open file: " << argv[1] <<endl;		exit(1);	}	fread(Imagedata, sizeof(unsigned char), Length*Width*BytesPerPixel, file);	fclose(file);	unsigned char ImageTrans[Length][Width][BytesPerPixel];	for(int i=0; i<Width; i++)		for(int j=0; j<Length; j++)		{			ImageTrans[i][j][0]=Imagedata[i][Length-j-1][0];			ImageTrans[i][j][1]=Imagedata[i][Length-j-1][1];			ImageTrans[i][j][2]=Imagedata[i][Length-j-1][2];		}	if (!(file=fopen(argv[2],"wb"))) {		cout << "Cannot open file: " << argv[2] << endl;		exit(1);	}	fwrite(ImageTrans, sizeof(unsigned char), Length*Width*BytesPerPixel, file);	fclose(file);	return 0;}